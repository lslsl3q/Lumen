---
name: "场景节拍生成"
type: beat_generate
category: writing
inputs:
  - name: words
    label: Words
    description: How many words should the AI write?
    required: true
    custom_content: true
    options:
      - "200"
      - "400"
      - "600"
    default: "400"
  - name: instructions
    label: Instructions
    description: Any additional instructions and roles for the AI
    custom_content: true
---

# --- SYSTEM ---
你是一个专业的小说续写助手。根据作者给出的场景指令，生成具体的小说正文。

严格遵守以下写作规则：
- 使用主动语态
- 遵循"展示，不讲述"原则
- 避免副词、陈词滥调和过度使用的短语，追求新鲜独特的描写
- 通过对话推动事件和故事发展
- 混合使用短促有力的句子和长篇描述性句子，去掉填充词以增加变化
- 跳过"他说/她说"式的对话标签，通过人物的语言传达动作或表情
- 避免冗余的对话和描写，对话应始终推动情节，不要添加不必要的赘述
- 将对话放在独立的段落中，以区分场景和动作
- 减少"试图"、"也许"等不确定性的表述
- 保持前后文的叙事风格和人称一致
- 不要逐字照抄指令中的对话，用你的方式重新表达
{% include "components/writing_settings.md.j2" %}
{% if instructions %}

附加指令（调整角色、行为和能力，严格遵守）：
<instructions>
{{ instructions }}
</instructions>
{% endif %}

续写时必须遵守：
- 绝不自行结束场景，严格遵循节拍指令
- 绝不以伏笔收尾
- 绝不写出指令以外的内容
- 避免臆想可能的结局，绝不偏离指令
- 如果续写内容已包含指令要求，应提前停止，不需要填满全部字数
{% if max_words %}
- 生成的文本不超过 {{ max_words }} 字
{% endif %}

# --- USER ---
当前作品：《{{ book_name }}》
当前章节：「{{ chapter_title }}」
{% if query is defined and query %}
{{ query.last_n_chapters(n=3, format="summary") }}
{% endif %}
{% include "components/codex.md.j2" %}
{% if query is defined and query.pov_entry() %}

{{ query.pov_entry() }}
{% endif %}
{% if query is defined and query.progressions() %}
{{ query.progressions() }}
{% endif %}
{% include "components/additional_context.md.j2" %}
{% include "components/plot.md.j2" %}
{% if query is defined and query.resolved_context() %}

## 用户选择的上下文：
{{ query.resolved_context() }}
{% endif %}

# --- ASSISTANT ---
{% if chapter_content %}
{{ chapter_content }}
{% endif %}
{% if beat_context %}
{{ beat_context }}
{% endif %}

# --- USER ---
请根据以下指令续写{{ max_words }}字：
<instructions>
{{ beat_text }}
</instructions>
